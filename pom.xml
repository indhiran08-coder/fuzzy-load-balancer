<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!--
        Parent: Spring Boot 3.2.x provides dependency management for all
        Spring ecosystem libraries, ensuring compatible versions are used.
        We use Java 21 (LTS) for virtual threads and modern language features.
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.fuzzybalancer</groupId>
    <artifactId>fuzzy-load-balancer</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Intelligent API Load Balancer using Fuzzy Logic</name>
    <description>
        A Spring Boot application that simulates a cloud load balancer
        capable of intelligently routing requests to the best backend
        server using Fuzzy Logic instead of traditional Round Robin.
    </description>

    <properties>
        <!-- Compile to Java 21 bytecode using JDK 25 as compiler.
             Spring Boot 3.2.5 supports Java 21 class files (not 25). -->
        <java.version>21</java.version>
        <!-- JWT library version -->
        <jjwt.version>0.12.5</jjwt.version>
        <!-- MapStruct for type-safe DTO mapping -->
        <mapstruct.version>1.5.5.Final</mapstruct.version>
        <!-- Springdoc OpenAPI for Swagger UI -->
        <springdoc.version>2.5.0</springdoc.version>
        <!-- Lombok 1.18.46 adds official Java 25 support -->
        <lombok.version>1.18.46</lombok.version>
    </properties>

    <dependencies>

        <!-- ============================================================
             CORE SPRING BOOT STARTERS
             ============================================================ -->

        <!--
            spring-boot-starter-web: Provides Spring MVC, embedded Tomcat,
            Jackson (JSON serialization), and REST support. This is the
            backbone of our REST API layer.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!--
            spring-boot-starter-data-jpa: Includes Spring Data JPA,
            Hibernate ORM, and Spring's JPA transaction management.
            Eliminates boilerplate DAO/Repository code via JpaRepository.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!--
            spring-boot-starter-security: Provides authentication,
            authorization, and security filters. We will configure it
            with stateless JWT-based auth.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!--
            spring-boot-starter-thymeleaf: Server-side HTML template engine
            for the admin dashboard. Integrates naturally with Spring MVC.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!--
            thymeleaf-extras-springsecurity6: Adds Thymeleaf dialect for
            Spring Security (e.g., sec:authorize tags in templates).
        -->
        <dependency>
            <groupId>org.thymeleaf.extras</groupId>
            <artifactId>thymeleaf-extras-springsecurity6</artifactId>
        </dependency>

        <!--
            spring-boot-starter-validation: Bean Validation (JSR-380) via
            Hibernate Validator. Used for @NotNull, @Size, @Email on DTOs.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!--
            spring-boot-starter-actuator: Production-ready endpoints for
            health, metrics, and monitoring (/actuator/health, etc.)
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- ============================================================
             DATABASE
             ============================================================ -->

        <!--
            mysql-connector-j: Official MySQL JDBC driver. Spring Boot
            auto-configures the DataSource when this is on the classpath
            and application.properties has the connection URL.
        -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!--
            h2: In-memory database for tests and local development.
            When profile is 'test', H2 is used; otherwise MySQL is used.
        -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ============================================================
             SECURITY — JWT
             ============================================================ -->

        <!--
            jjwt-api: JJWT (Java JWT) API — defines the JWT builder and
            parser interfaces. Industry-standard library for JWT handling.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>

        <!--
            jjwt-impl: JJWT implementation (runtime only).
            Actual signing and parsing logic lives here.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!--
            jjwt-jackson: JJWT Jackson integration for JSON serialization
            of JWT claims. Required for JJWT 0.12+.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- ============================================================
             UTILITIES
             ============================================================ -->

        <!--
            lombok: Annotation processor that generates boilerplate Java
            code at compile time: @Getter, @Setter, @Builder, @Slf4j, etc.
            Drastically reduces verbosity.
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!--
            mapstruct: Compile-time type-safe mapper generator.
            Converts between Entity ↔ DTO without reflection overhead.
            Far safer and faster than ModelMapper at runtime.
        -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct-processor</artifactId>
            <version>${mapstruct.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- ============================================================
             API DOCUMENTATION — SWAGGER / OPENAPI
             ============================================================ -->

        <!--
            springdoc-openapi-starter-webmvc-ui: Auto-generates OpenAPI 3
            spec from Spring MVC annotations. Serves interactive Swagger UI
            at /swagger-ui.html. Replaces the old springfox library.
        -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- ============================================================
             TESTING
             ============================================================ -->

        <!--
            spring-boot-starter-test: Includes JUnit 5, Mockito, AssertJ,
            MockMvc, and Spring Test Context. Everything needed for unit
            and integration tests.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
            spring-security-test: Provides SecurityMockMvcConfigurers and
            @WithMockUser for testing secured endpoints without real auth.
        -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!--
                spring-boot-maven-plugin: Packages the app as an executable
                fat JAR with all dependencies embedded. Enables 'mvn spring-boot:run'.
                Excludes Lombok from the final artifact (it's compile-only).
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!--
                maven-compiler-plugin: Configured for Java 21 with
                annotation processors for Lombok and MapStruct.
                The order matters: Lombok must run BEFORE MapStruct
                so MapStruct can see Lombok-generated getters/setters.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>0.2.0</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
